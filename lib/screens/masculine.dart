import 'package:flutter/material.dart';

class MasculineScreen extends StatelessWidget {
  const MasculineScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}